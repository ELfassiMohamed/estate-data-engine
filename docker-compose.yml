services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: redis-estate-data-engine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: